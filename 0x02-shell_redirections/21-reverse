#!/bin/bash

# Check if there's input
if [ -z "$1" ]; then
    echo "Usage: $0 <string>"
    exit 1
fi

# Reverse the input using rev command
reversed=$(echo "$1" | rev)

# Print the reversed input
echo "$reversed"
